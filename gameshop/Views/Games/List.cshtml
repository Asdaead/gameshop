@using gameshop.ViewModels
@model GamesListViewModel


    <h2>
        Все игры
    </h2>
    <h3>@Model.currCategory</h3>
    <div class="row mt-5 mb-2">
       @{
            foreach (Game game in Model.allGames)
            {
                @Html.Partial("AllGames", game);
            }
        }
        </div>
